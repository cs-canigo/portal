box: node:4.2.6-slim
build:
  steps:
    - arjen/hugo-build@1.13.1:
        version: "0.17"
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: canigo.ctti.gencat.cat
        basedir: public
    - script:
        name: echo nodejs information
        code: |
          echo "npm install markdown2json"        
          echo "npm install algoliasearch"                  